#include "main.h"
#include <stdio.h>

/**
 * main - function that cjecks uppercase character.
 *
 * Return: 1 if  c is uppercase, otherwise 0.
 */
int main(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	return (0);
}
